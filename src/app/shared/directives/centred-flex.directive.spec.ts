import { CentredFlexDirective } from './centred-flex.directive';

describe('CentredFlexDirective', () => {
  it('should create an instance', () => {
    const directive = new CentredFlexDirective();
    expect(directive).toBeTruthy();
  });
});
